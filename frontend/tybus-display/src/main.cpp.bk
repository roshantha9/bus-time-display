#include <Arduino.h>

void setup() {
  // setup pin 5 as a digital output pin
  //pinMode (ledPin, OUTPUT);
  Serial.begin(115200);
}

void loop() {
  // put your main code here, to run repeatedly:
  Serial.print("hellloo \n");

  delay(1000);	// wait for half a second or 500 milliseconds
}